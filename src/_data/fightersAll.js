// Every fighter, in manifest order. `fightersByCategory` groups them for the
// directory page using the canonical category order in data/fighters.json.
module.exports = require("../../data/fighters.json").fighters;
