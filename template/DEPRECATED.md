# Deprecated — do not stamp new puzzles from these files

As of the July 2026 Eleventy migration, puzzle pages are **generated** from
`data/puzzles.json` by the templates in `src/` (see CLAUDE.md → Common Tasks).
These standalone HTML templates are kept only as historical reference for the
arcade format, which itself is archived at `/v1/` and on `/compare/`.
